namespace LogReplace
{
    class Program
    {
        static void Main(string[] args)
        {
            // 文字列の置換処理を行う
            LogReplace logReplace = new LogReplace();
            logReplace.ReplaceContentForKey();
        }
    }
}
